<!DOCTYPE html>
<html lang="pt-br">

<head>
<meta charset="UTF-8" />
<meta http-equiv="X-UA-Compatible" content="IE=edge" />
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <%- include('../templates/head.ejs') %>
  <link rel="stylesheet" href="/css/cadastro.css"/>
  <script type="text/javascript" src="/js/cadastro.js"></script>
  <title><%- data.page_name %></title>
</head>

<body>
  <%- include('../templates/header-logo.ejs') %>

  <main>

        <div id="voltar">
                <a href="/perfil-user">
                <i class='bx bx-left-arrow-alt'></i>
                </a>
        </div>

    <h3>SEU CADASTRO</h3>

    <form action="/editar-perfil" method="post">
              <%- data.errors && data.errors.sistema_error ? `<span class='erro-mensagem'>${data.errors.sistema_error.msg}</span>` : "" %>

              <label for="nome">NOME:</label>
      <input type="text" placeholder="Insisa aqui..." name="nome" id="nome"
              <%- data.input_values ? `value="${data.input_values.nome}"` : "" %>
              <%- data.errors && data.errors.nome_error ? `class="erro"` : "" %>
              /><br />

              <%- data.errors && data.errors.nome_error ? `<span class='erro-mensagem'>${data.errors.nome_error.msg}</span>` : "" %>
              <br />

      <label for="nome_de_usuario">NOME DE USUÁRIO:</label>
      <input type="text" placeholder="Insisa aqui..." name="nome_de_usuario" id="nome_de_usuario"
              <%- data.input_values ? `value="${data.input_values.nome_de_usuario}"` : "" %>
              <%- data.errors && data.errors.nome_de_usuario_error ? `class="erro"` : "" %>
              /><br />

              <%- data.errors && data.errors.nome_de_usuario_error ? `<span class='erro-mensagem'>${data.errors.nome_de_usuario_error.msg}</span>` : "" %>
              <br />

      <label for="telefone">TELEFONE:</label>
      <input type="tel" placeholder="(00) 99999-9999" maxlength="15"  name="telefone" id="telefone" onkeyup="handlePhone(event)"
              <%- data.input_values ? `value="${data.input_values.telefone}"` : "" %>
              <%- data.errors && data.errors.telefone_error ? `class="erro"` : "" %>
              /><br />

              <%- data.errors && data.errors.telefone_error ? `<span class='erro-mensagem'>${data.errors.telefone_error.msg}</span>` : "" %>
              <br />

      <label for="email">E-MAIL:</label>
      <input type="email" placeholder="exemplo@email.com" name="email" id="email"
              <%- data.input_values ? `value="${data.input_values.email}"` : "" %>
              <%- data.errors && data.errors.email_error ? `class="erro"` : "" %>
              /><br />

              <%- data.errors && data.errors.email_error ? `<span class='erro-mensagem'>${data.errors.email_error.msg}</span>` : "" %>
              <br />

      <legend>ENDEREÇO</legend>

              <div>
                  <div id="nomeRua">
      <label for="logradouro">LOGRADOURO:</label>
      <input type="text" placeholder="Insisa aqui..." name="logradouro" id="logradouro"
              <%- data.input_values ? `value="${data.input_values.logradouro}"` : "" %>
              <%- data.errors && data.errors.logradouro_error ? `class="erro"` : "" %>
              />

              <%- data.errors && data.errors.logradouro_error ? `<span class='erro-mensagem'>${data.errors.logradouro_error.msg}</span>` : "" %>
                  </div>
                  <div id="numeroResidencial">
      <label for="numero_residencial">N°</label>
      <input type="number" placeholder="000" name="numero_residencial" id="numero_residencial"
              <%- data.input_values ? `value="${data.input_values.numero_residencial}"` : "" %>
              <%- data.errors && data.errors.numero_residencial_error ? `class="erro"` : "" %>
              />
              
              <%- data.errors && data.errors.numero_residencial_error ? `<span class='erro-mensagem'>${data.errors.numero_residencial_error.msg}</span>` : "" %>
              
                   </div>
              </div>

      <label for="bairro">BAIRRO:</label>
      <input type="text" placeholder="Insisa aqui..." name="bairro" id="bairro"
              <%- data.input_values ? `value="${data.input_values.bairro}"` : "" %>
              <%- data.errors && data.errors.bairro_error ? `class="erro"` : "" %>
              /><br />

              <%- data.errors && data.errors.bairro_error ? `<span class='erro-mensagem'>${data.errors.bairro_error.msg}</span>` : "" %>
              <br />

      <label for="cidade">CIDADE:</label>
      <input type="text" placeholder="Insisa aqui..."  name="cidade" id="cidade"
              <%- data.input_values ? `value="${data.input_values.cidade}"` : "" %>
              <%- data.errors && data.errors.cidade_error ? `class="erro"` : "" %>
              /><br />

              <%- data.errors && data.errors.cidade_error ? `<span class='erro-mensagem'>${data.errors.cidade_error.msg}</span>` : "" %>
              <br />

      <label for="estado">ESTADO:</label>
      <input type="text" placeholder="Insisa aqui..."  name="estado" id="estado"
              <%- data.input_values ? `value="${data.input_values.estado}"` : "" %>
              <%- data.errors && data.errors.estado_error ? `class="erro"` : "" %>
              /><br />

              <%- data.errors && data.errors.estado_error ? `<span class='erro-mensagem'>${data.errors.estado_error.msg}</span>` : "" %>
              <br />

      <label for="cep">CEP:</label>
      <input type="text" placeholder="00000-000" name="cep" id="cep" maxlength="9" onkeyup="handleZipCode(event)"
              <%- data.input_values ? `value="${data.input_values.cep}"` : "" %>
              <%- data.errors && data.errors.cep_error ? `class="erro"` : "" %>
              /><br />

              <%- data.errors && data.errors.cep_error ? `<span class='erro-mensagem'>${data.errors.cep_error.msg}</span>` : "" %>
              <br />
              <input type="submit" id="cadastre-se" value="ATUALIZAR"></input>
    </form>

        <a href="/deletar-perfil" class="delete-perfil">
        <i class='bx bxs-trash'></i><p>Deletar Perfil</p>
        </a>
  </main>

  <%- include("../templates/footer.ejs") %>
</body>
</html>